# Web App URL configuration
VITE_API_URL=http://localhost:8000

# For local development, the web app runs on port 5174 by default (Vite)
# For production, this should be your actual web app URL (e.g., https://app.legacy.com)
